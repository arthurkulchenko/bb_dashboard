.jumbotron.jumbotron-fluid
  .container
    %h2.display-4 Burning Buttons Dashboard
.container
  %table.table
    %thead
      %tr
        %th Full name
        %th Reason of absence
        %th Contacts
    - @employees.each do |e| 
      %tr
        %td= e.full_name
        %td= e.occupations.empty? ? "should be on work" : e.occupations.prepare
        %td= "e.profile.contacts"